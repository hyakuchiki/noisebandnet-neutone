_target_: noisebandnet.ddsp.data.SmallDataset

raw_path: null
sample_rate: ${sample_rate}
length: 1.487
frame_rate: 1378
dataset_size: 6400
feat_proc:
  _target_: noisebandnet.ddsp.features.FeatureProcessor
  features:
    centroid:
      _target_: noisebandnet.ddsp.features.SpectralCentroid
      sample_rate: ${sample_rate}
      window_size: 1024
      frame_rate: ${data.frame_rate}
      center: True
      n_fft: 1024
    volume:
      _target_: noisebandnet.ddsp.features.Volume
      sample_rate: ${sample_rate}
      window_size: 1024
      frame_rate: ${data.frame_rate}
      center: True